apiVersion: v1
kind: Service
metadata:
  name: {{ include "partners-bff.fullname" . }}
  labels:
    {{- include "partners-bff.labels" . | nindent 4 }}
  annotations: {}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "partners-bff.selectorLabels" . | nindent 4 }}