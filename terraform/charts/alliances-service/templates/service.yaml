apiVersion: v1
kind: Service
metadata:
  name: {{ include "alliances-service.fullname" . }}
  labels:
    {{- include "alliances-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "alliances-service.selectorLabels" . | nindent 4 }}